services:
  backend:
    build: .
    ports:
      - "8000:8000"
  frontend:
    build: ./web
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      - API_URL=http://backend:8000

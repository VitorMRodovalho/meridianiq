services:
  meridianiq-api:
    build: .
    ports:
      - "8000:8000"
  meridianiq-web:
    build: ./web
    ports:
      - "3000:3000"
    depends_on:
      - meridianiq-api
    environment:
      - API_URL=http://meridianiq-api:8000
